build/
out/
boardcommd
boardcomm_pub
boardcomm_sub
log/*.log
*.bin

# archives (exceed GitHub 100MB limit; use deps/boost_1_84_0 source instead)
*.tar.gz
*.tar.bz2

# Boost b2 intermediate build output inside source tree (not source code)
deps/boost_*/bin.v2/
deps/boost_*/stage/
